apiVersion: v1
kind: Service
metadata:
  name: trainer-service
  namespace: default
  labels:
    app: trainer-server
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: trainer-server
